# Ansible Integration

## ansible
The directory for ansible where `site.yml` is the playbook that is ran.

## s3_upload.tf
A Terraform script to upload `ansible` to an S3 bucket under the `bootstrap` key.